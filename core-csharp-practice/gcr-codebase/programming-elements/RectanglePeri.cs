class RectanglePeri
{
    static void Main(string[] args)
    {
        double length = 10.0;
        double width = 5.0;
        double perimeter = 2 * (length + width);
        System.Console.WriteLine("The perimeter of the rectangle is: " + perimeter);
    }
}